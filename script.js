document.getElementById("timestamp").textContent = new Date().toLocaleString();

// You can also dynamically update statuses here in the future
// e.g., fetch('/status.json') and apply classes accordingly
